export enum RepairStatus {
  WAITING = 'WAITING',
  DIAGNOSIS = 'DIAGNOSIS',
  WAITING_CUSTOMER_APPROVAL = 'WAITING_CUSTOMER_APPROVAL',
  IN_PROGRESS = 'IN_PROGRESS',
  READY = 'READY',
  DELIVERED = 'DELIVERED',
  CANCELLED = 'CANCELLED',
}
